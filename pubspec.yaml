name: irunner
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.0
  extended_image: 2.0.0 #图片
  flutter_screenutil: 4.0.4+1 #屏幕适配
  get: 3.26.0 #状态管理
  images_picker: 0.0.9 #相机及相册
#  amap_map_fluttify: ^0.30.1
  dio: 3.0.10 #网络请求
  sqflite: 1.3.2+4 #数据库
  intl: 0.16.1 #国际化
  event_bus: 1.1.1 #消息总线
  flutter_baidu_mapapi_map: 2.0.1
  oktoast: 2.3.2 #toast，对话框

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/fonts/